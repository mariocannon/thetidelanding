# Supabase project config for The Tide.
# Project ref: jykpoupjvcmvoihujfkc (ap-southeast-2)
project_id = "jykpoupjvcmvoihujfkc"

[functions.beehiiv-sync]
# Called directly by the public signup form in the browser, so JWT
# verification is off. The function validates the email and CORS itself,
# and the Beehiiv API key lives only in the function's secrets.
verify_jwt = false
